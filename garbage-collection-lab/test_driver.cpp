#include "test_driver.h"

void load_trace_files() {
    
}

void test_driver(char** trace_files) {
    if (trace_files == NULL) {
        printf("Lack of Trace file\n");
        return;
    }
    for (trace_files; *trace_files; trace_files++) {
        
    }
}